import { StaticPage } from "@/components/public/static-page";

export default function CookiesPage() {
  return (
    <StaticPage title="Politica cookies">
      <p>Aplicația folosește cookie-uri strict necesare pentru autentificare și coș local în browser.</p>
      <p>Nu folosim servicii externe de plată sau tracking în această versiune locală.</p>
    </StaticPage>
  );
}
